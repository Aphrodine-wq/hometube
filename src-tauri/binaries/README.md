# xytz sidecar

Run `../../scripts/fetch-xytz.sh` before creating a release bundle. The script
downloads a pinned Linux x86_64 xytz release into this directory using Tauri's
required target-triple filename.
